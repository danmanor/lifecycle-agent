---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: ipconfigs.lca.openshift.io
spec:
  group: lca.openshift.io
  names:
    kind: IPConfig
    listKind: IPConfigList
    plural: ipconfigs
    shortNames:
    - ipc
    singular: ipconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .spec.stage
      name: Desired Stage
      type: string
    - jsonPath: .status.conditions[-1:].reason
      name: State
      type: string
    - jsonPath: .status.conditions[-1:].message
      name: Details
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: IPConfig is the Schema for controlling node IP configuration
          lifecycle via lca-cli ip-config.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IPConfigSpec defines the desired state of IPConfig
            properties:
              ipv4:
                description: IPv4 stack (omit for IPv6-only)
                properties:
                  address:
                    description: Address is the full address with prefix length (e.g.,
                      192.0.2.10/24)
                    type: string
                  dnsServer:
                    description: DNSServer is the DNS server IP to use
                    type: string
                  gateway:
                    description: Gateway is the default gateway address
                    type: string
                  machineNetwork:
                    description: MachineNetwork is the CIDR of the machine network
                      (e.g., 192.0.2.0/24)
                    type: string
                type: object
              ipv6:
                description: IPv6 stack (omit for IPv4-only)
                properties:
                  address:
                    description: Address is the full address with prefix length (e.g.,
                      192.0.2.10/24)
                    type: string
                  dnsServer:
                    description: DNSServer is the DNS server IP to use
                    type: string
                  gateway:
                    description: Gateway is the default gateway address
                    type: string
                  machineNetwork:
                    description: MachineNetwork is the CIDR of the machine network
                      (e.g., 192.0.2.0/24)
                    type: string
                type: object
              proxy:
                description: Optional proxy settings
                properties:
                  httpProxy:
                    type: string
                  httpsProxy:
                    type: string
                  noProxy:
                    items:
                      type: string
                    type: array
                type: object
              pullSecretRef:
                description: pullSecretRef is the name of a Secret in the openshift-config
                  namespace containing .dockerconfigjson
                type: string
              recertImage:
                description: Recert image for certificate rotation during configure
                  stage
                type: string
              stage:
                description: IPConfigStage defines the type for the stage field
                enum:
                - Idle
                - Prepare
                - Configure
                - Rollback
                type: string
              vlan:
                description: Optional VLAN applied to br-ex path
                properties:
                  id:
                    type: integer
                type: object
            type: object
          status:
            description: IPConfigStatus defines the observed state of IPConfig
            properties:
              clusterIPs:
                description: ClusterIPs reflects the node internal IPs known to the
                  cluster
                properties:
                  nodeInternalIPs:
                    items:
                      description: FamilyIP pairs IP family and address
                      properties:
                        address:
                          type: string
                        family:
                          type: string
                      required:
                      - address
                      - family
                      type: object
                    type: array
                type: object
              conditions:
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
              validNextStages:
                description: ValidNextStages enumerates allowed next transitions from
                  current stage
                items:
                  description: IPConfigStage defines the type for the stage field
                  type: string
                type: array
            type: object
        type: object
        x-kubernetes-validations:
        - message: ipconfig is a singleton, metadata.name must be 'ipconfig'
          rule: self.metadata.name == 'ipconfig'
    served: true
    storage: true
    subresources:
      status: {}
